name: Node

on:
  - push
  - pull_request
env:
  Node: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          cache: 'npm'
      - run: make install
      - run: make lint
      - run: make test
